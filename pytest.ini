[pytest]
markers =
    integration: requires a live PostgreSQL test DB (barack_corp_test via .env.test)
    stress: concurrency/load scenario, excluded by default — run explicitly with -m stress
addopts = -m "not stress"
